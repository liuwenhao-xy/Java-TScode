# 35-搜索插入位置

# 题目要求：

给定一个排序数组和一个目标值，在数组中找到目标值，并返回其索引。如果目标值不存在于数组中，返回它将会被按顺序插入的位置。

请必须使用时间复杂度为 `O(log n)` 的算法。

**示例 1:**

**输入:** nums = [1,3,5,6], target = 5
**输出:** 2

**示例 2:**

**输入:** nums = [1,3,5,6], target = 2
**输出:** 1

**示例 3:**

**输入:** nums = [1,3,5,6], target = 7
**输出:** 4

**提示:**

* `1 <= nums.length <= 104`
* `-104 <= nums[i] <= 104`
* `nums` 为 **无重复元素** 的 **升序** 排列数组
* `-104 <= target <= 104`

这道题通过Java和typescript两种语言来写的时候，要注意两个语言的代码格式，基本实现思想都差不多

我们可以通过二分法的查找方式来实现，也可以通过基本查询的方式来实现

二分法的要求是数组必须是升序数组

## 1)Java来实现

```java
class Solution {
    public int searchInsert(int[] nums, int target) {
        //定义初始索引和结束索引，注意结束索引应该是数组长度-1
        int start = 0;
        int end = nums.length - 1;
        //通过while循环进行二分法的查找，当起始索引不比结束索引大的时候，一直在循环中进行遍历查找
        while(start <= end){
            // 计算中间索引值，end - start可以避免数组的越界
            int mid = start + (end - start) / 2;
            //当target=nums[mid]时，说明已经找到了目标值，此时直接返回当前索引mid即可
            if(target == nums[mid]){
                return mid;
            }else if(target > nums[mid]){
                //当target > nums[mid]时，说明此时target在当前mid索引的右边，所以下一次查找时，起始索引是mid+1
                start = mid + 1;
            }else if(target < nums[mid]){
                //当target < nums[mid]时，说明此时target在当前mid索引的左边，所以下一次查找时，结束索引是mid - 1
                end = mid - 1;
            }
        }
        //当我们整个循环遍历结束之后仍然没有找到我们的target，注意此时的索引是start
        return start;
    }
}
```

## 2)TypeScript来实现

这里要注意ts代码数学算法的语法格式，否则会超出代码运行的时间限制

```ts
function searchInsert(nums: number[], target: number):number{
    //使用二分法进行查找
    //定义起始索引，结束索引，还是要注意结束索引的取值范围
    let start:number = 0;
    let end:number = nums.length - 1;
    //通过while循环进行遍历查找，查找的条件是start<=end，在这个条件下不断地进行遍历
    while(start <= end){
        //定义中间索引，由于let定义的是变量，所以中间值可以使用const来定义
        //这里要规范ts代码的书写，否则代码的运行会超出时间的限制
        const mid:number = start + Math.floor((end - start) / 2);//end - start这样可以避免数组越界问题
        //当target=nums[mid]时，说明已经在数组中找到了target，我们就直接返回当前索引mid
        if(target == nums[mid]){
            return mid;
        }else if(target > nums[mid]){
            //当target>nums[mid]时,说明此时的目标值还在中间值的右边，那么接下来的起始索引 = mid + 1
            start = mid + 1;
        }else if(target < nums[mid]){
            //当target<nums[mid]时,说明此时的目标值还在中间值的左边，那么接下来的结束索引 = mid - 1
            end = mid - 1;
        }
    }
    //当我们整个循环遍历结束之后，target不存在，题目要求返回的是按顺序插入的位置，就应该是start
    return start;
}
```

## 3)TypeScript的基本查询

```ts;
function searchInsert(nums: number[], target: number):number{
    /*
    * typescript 定义方法
          1.  const searchInsert()=>{}
          2.  function searchInsert(){}
    * nums: number[] 是一个数组， target是一个独立的值。 返回的是一个number类型的数值
    */
    for (let index = 0; index < nums.length; index++) {
        // index代表下角标
        // cur_num代表当前的值
        // 提示一下， let 代表在一个方法内可以变动的值 const 代表在一个方法内不能变动的值。
        let cur_num = nums[index];
        // 匹配到结果相等 
        if(cur_num==target){
            return index;
        }
        // 因为整个数组是按照顺序排放的，如果有数字比现在的数字大，后面就不可能在出现相等的了。
        // 只需要在比他大的第一个数的前面，出现就好了。
        if(cur_num>target){
            return index;
        }
    }
    // 排序到最后如果没有找到，就排序到最后，出现这样的结果。
    return  nums.length;
}
```

分享一下我自己的一些心得吧：

    二分法呢，理解起来还是比较容易的，但是想要把代码逻辑写的不出错还是要细心一点，对整个题目的逻辑要分析的透彻一些，

比如，对于target不在数组中的时候，为什么要返回的是start索引，而不是end索引，先看题目要求，他要在不存在的时候，返回的是按顺序插入的下一个位置，start停止的时候恰好就是他的自然停止位置，当你返回的是end的时候，如果target是7，那么程序就会报错
