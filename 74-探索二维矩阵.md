# 74-探索二维矩阵

## 题目描述

给你一个满足下述两条属性的 `m x n` 整数矩阵：

* 每行中的整数从左到右按非严格递增顺序排列。
* 每行的第一个整数大于前一行的最后一个整数。

给你一个整数 `target` ，如果 `target` 在矩阵中，返回 `true` ；否则，返回 `false` 。

![2924dc02-f237-45e5-aa75-a1b2f835862f](file:///C:/Users/hp/Pictures/Typedown/2924dc02-f237-45e5-aa75-a1b2f835862f.png)

* https://assets.leetcode.com/uploads/2020/10/05/mat.jpg

* 输入：matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 3

* 输出：true

![f2a3ce75-0069-4ff2-ab8c-2956034055af](file:///C:/Users/hp/Pictures/Typedown/f2a3ce75-0069-4ff2-ab8c-2956034055af.png)

* https://assets.leetcode-cn.com/aliyun-lc-upload/uploads/2020/11/25/mat2.jpg

* 输入：matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 13

* 输出：false

通过这道题率先可以判断出，数组都是升序的，且，每一行的首位数字大于上一行的结束数字；通过这两个

条件，我们可以得到通过二分查找来解这道题

1.我们可以先使用二分查找对第一列进行遍历，找到target所对应的那一行

2.我们可以对这一行的元素，再次进行二分法的查找，找到target

3.如果我们都没有找到，说明target不存在，根据题目中函数的返回类型，返回false就可以了

我通过Java和TypeScript两种语言来写

## 1)Java

```java
class Solution {
    public boolean searchMatrix(int[][] matrix, int target) {
        /**
            1.因为数组是升序的，且第二行开始的这个数字一定要比前一行的所有数字都要大，所以，我们可以通过
                率先遍历列的方式，找到target所在的行，因为列也是升序排列的，所以我们也可以用二分查找
            2.当我们找到这个target所在的行时，我们可以对这一行进行二分查找，定义start，end，mid索引，当行里面没有找到
                target时，就说明，不存在target，我们就返回false就可以了
         */ 

        int m = matrix.length;//就是我们要找的列
        int n = matrix[0].length;//就是从第一行开始找

        //我们先通过二分查找，找到target所对应的是哪一行
        //定义起始索引和结束索引
        int top = 0;
        int bottom = matrix.length - 1;;
        //因为遍历时都是索引，所以结束索引要是数组长度-1
        //通过while循环进行查找，条件就是当top < bottom就一直在找target所在的那一行
        while(top < bottom){
            //进入循环，要找到中间索引，注意这里中间索引的取值
            int mid = (top + bottom + 1) / 2; 
            //让target和matrix[mid][0]进行比较
            if(target >= matrix[mid][0]){
                //此时就说明已经找到这个target了
                top = mid;
                //target > matrix[mid][0]时，也是返回top = mid，因为他就算比这一行所有的数据都大，那么在进行下一轮
                //循环时，也会直接返回
            }else {
                //target < matrix[mid][0]时，说明target在当面mid的上面一行，所以top不变，bottom = mid - 1
                bottom =  mid - 1;
            }
        }
        //现在我们已经找到了target所在的行，我们对这一行进行二分查找
        //定义起始索引和结束索引，我们当前行就是top
        int row = top;
        int start = 0,end = n - 1;
        //此时我们就要用while循环进行查找，条件是start <= end，就在这一行查找
        while(start <= end){
            //计算中间索引，这里就和我们35题一样，中间索引要避免越界
            int mid = start + (end - start) / 2;
            //此时，我们让target和matrix[row][mid]进行比较
            if(target > matrix[row][mid]){
                //当target > matrix[row][mid]时，target在mid的右边，此时，end不变，start + 1
                start = start + 1;
            }else if(target < matrix[row][mid]){
                //当target < matrix[row][mid]时，target在mid的左边，此时，start不变，end - 1
                end = end - 1;
            }else{
                //当target = matrix[row][mid]，就说明已经找到了target，函数返回值是布尔类型
                return true;
            }

        }
        //当我们没有找到target时，就说明数组中不存在
        return false;
    }
}
```

## 2)TypeScript

```ts
function searchMatrix(matrix: number[][], target: number): boolean {
    let m:number = matrix.length//这是要遍历的列
    let n:number = matrix[0].length//这是我们要找的行
    //注意方法的返回值是布尔类型
    //1.我们先对第一列进行二分查找，我们找到target所对应的那一行
    //定义起始索引，结束索引
    let top: number = 0;
    let bottom: number = m - 1//注意是比较的索引值，要是长度-1

    //我们通过while循环对第一列进行遍历，找到target所对应的那一行
    while(top < bottom){
        //定义中间值mid
        let mid:number = Math.floor((top + bottom + 1) / 2);
        if(target < matrix[mid][0]){
            //当target < matrix[mid][0]时，说明此时target所在行在mid的上一行，，top不变，bottom = mid - 1
            bottom = mid - 1;
        }else if(target >= matrix[mid][0]){
            //就说明我们已经找到了target所在的行，有可能在当前行，也有可能在下一行，但是我们通过while循环，可以继续查找
            top = mid;
        }
    }
    
    //接下来我们对当前行进行二分查找，此时行 = top
    let row:number = top;
    //定义起始索引，和结束索引
    let start:number = 0；
    let end:number = n - 1;
    while (start <= end) {
        //计算中间值mid时，要注意越界问题
        let mid:number = start + Math.floor((end - start) / 2);
        if (matrix[row][mid] == target) {
            //当matrix[row][mid] == target时，就说明此时已经找到了target，根据函数返回类型，我们直接返回true
            return true;
        } else if (matrix[row][mid] < target) {
            //当matrix[row][mid] < target时，说明target在mid的右边，end不变，start + 1
            start = mid + 1;
        } else {
            //当matrix[row][mid] > target时，说明target在mid的左边，start不变，end -  1
            end = mid - 1;
        }
    }
    //当target不存在时，我们返回false
    return false;
};
```

## 空间复杂度为O(1)

## 时间复杂度为O(Logn+Logm)

## 自己对这道题目的理解：

    这道题有其他的解法，但是当我们拿到手进行分析的时候，根据矩阵中数字的排序方式，其实应该先想到二分法查找，

这个方法就是能让你的思路变得更清除一些，无非就是两种语言在语法的略微差别，整体逻辑都差不多。我认为这道题二分法的难点就是计算中间值

，我们要避免mid越求越大的问题，所以中间值的计算要靠考虑一下。
