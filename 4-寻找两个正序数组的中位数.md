# 题目描述：

给定两个大小分别为 `m` 和 `n` 的正序（从小到大）数组 `nums1` 和 `nums2`。请你找出并返回这两个正序数组的 **中位数** 。

算法的时间复杂度应该为 `O(log (m+n))` 。

**示例 1：**

输入：**nums1 = [1,3], nums2 = [2]**
输出：2.00000
解释：合并数组 = [1,2,3] ，中位数 2

**示例 2：**

输入：**nums1 = [1,2], nums2 = [3,4]**
输出：2.50000
解释：合并数组 = [1,2,3,4] ，中位数 (2 + 3) / 2 = 2.5

**提示：**

* `nums1.length == m`
* `nums2.length == n`
* `0 <= m <= 1000`
* `0 <= n <= 1000`
* `1 <= m + n <= 2000`
* `-106 <= nums1[i], nums2[i] <= 106`

题目解析：

    这道题其实拿到手一看，挺简单的，就是把两个数组合并起来，再重新排序一下就好了，但是我们要看时间复杂度`O(log (m+n))`，从log可以得出

我们就要用二分查找来进行，要优化我们的时间复杂度。

    中位数：数组长度和为偶数时，我们就要返回中间两个数的平均值；为奇数时，我们就要返回中间的那个数。那么中间的数怎么进行查找，就是我们的关键。因为有**nums1**和**nums2**两个数组，所以我们要对两个数组进行分割，分成左边和右边两个区域进行查找。

    我们来假设两个数据多一点的数组来看：

             [1        3        5        7        9]

             [2        4        6        8        10]

    我们看这两个数组，各自都是排序的，满足题目的要求。那么我们怎么来分割呢，先整体来看就是1-10这10个数字，那么56的平均值就是中位数，怎么来找呢？在分割的时候，我们的条件是，左侧的元素 < 右侧的元素；左侧的元素个数 < 右侧的元素个数；

     按照这个条件来看，nums1就应该是(1+5)/2 = 2，分割线在5这个位置；那么就是****(left + right) / 2 = i****，也可以是(0 + m - 1 ) / 2，

      nums2(5 + 5 + 1) / 2 - 2 =  3，这个分割线在6位置，那么就是(m + n + 1) / 2 - i = j； 

    接下来我们要用while循环来查找分割线的位置，并且获取分割线左右两侧的数据，验证我们的分割线位置是否正确

    **我用Java和TypeScript两种语言来实现！**

## 1)Java

```java
class Solution{
    public double findMedianSortedArrays(int[] nums1, int[] nums2) {
        //我们在查找的时候，把短数组放在长数组的前面，这样子我们的查找效率会高一点
        if(nums1.length > nums2.length){
            int[] temp = nums1;
            nums1 = nums2;
            nums2 = temp;
        }//这个时候我们就把短一点的数组放前面来了
         //我们要定义这个两个数组的长度
        int m = nums1.length;
        int n = nums2.length;
        //接下来我们要给两个数组都找到分割线，把两个数组从内部分开
        //左侧部分 < 右侧部分元素；左侧部分的个数 = 右侧部分的个数（或者左侧比右侧多一个数）
        int left = 0;
        int right = m;//从0开始，从数组长度结束，因为是分割线，所以还是要占一个位置的，在这里长度不用-1
        //我们进入循环开始进行查找
        while(left <= right){
            //确保不漏掉任何一个元素

            //先确定nums1的分割线1 3 5 7 9
            int i = (left + right) / 2; //-----> 2位置
            //再确定nums2的分割线 2 4 6 8 10
            //左侧部分的个数 = 右侧部分的个数（或者左侧比右侧多一个数）
            int j = (m + n + 1) / 2 - i;

            // 获取分割线左右的元素
            double maxLeft1;
            if (i == 0) {
                maxLeft1 = Integer.MIN_VALUE;
            } else {
                maxLeft1 = nums1[i - 1];
            }

            double minRight1;
            if (i == m) {
                minRight1 = Integer.MAX_VALUE;
            } else {
                minRight1 = nums1[i];
            }

            double maxLeft2;
            if (j == 0) {
                maxLeft2 = Integer.MIN_VALUE;
            } else {
                maxLeft2 = nums2[j - 1];
            }

            double minRight2;
            if (j == n) {
                minRight2 = Integer.MAX_VALUE;
            } else {
                minRight2 = nums2[j];
            }   


            //我们获取到元素之和，就要判断当前的分割线是否是正确的
            // 判断是否找到了正确的分割线
            if (maxLeft1 <= minRight2 && maxLeft2 <= minRight1) {
                // 找到合适的分割线，计算中位数
                if ((m + n) % 2 == 0) {
                    return (Math.max(maxLeft1, maxLeft2) + 
                            Math.min(minRight1, minRight2)) / 2.0;
                } else {
                    return Math.max(maxLeft1, maxLeft2);
                }
            }
            // 调整搜索范围
            else if (maxLeft1 > minRight2) {
                right = i - 1;
            }
            else {
                left = i + 1;
            }
        }
        return 0.0;
        
    }
}
```

## 2)TypeScript

```ts
function findMedianSortedArrays(nums1: number[], nums2: number[]): number {
    /** 
        两个有序的排序数组，需要我们求出他的中位数，那么中位数就是有两种情况，当两个数组的长度之和为偶数时，就是返回
    中间两个数的平均数；当为奇数时，就是返回中间位置的数就好。
        我们通过对题目的分析，只要是时间复杂度中出现了log，那么我们就需要考虑用二分查找进行处理。我们如何在不合并两个数组
    的前提下，然后还能找到这个中位数，我们就需要把题目中所给的两个数组都进行划分，左边区域我们找最小值，右边区域我们找的是
    最大值，这样就可以在数组为偶数的时候，返回左边和右边区域两个数的平均数。然后为奇数的时候，直接返回左边的值就好了。
        其实这个分割线就是把数组分成了四个区域，这四个区域：左1，右1，左2，右2这四个数字拿出来刚好是排序的
        分割线的位置该怎么确定呢？左半部分的元素 < 右半部分的元素； 
                                左半部分的元素个数 = 右半部分的个数（或者比右边多一个）
    */
    //1.为了我们查找的效率和我们的逻辑清晰，我们把短数组放在前面
    if(nums1.length > nums2.length){
       //ts的交换位置比Java来的更明显
       [nums1,nums2] = [nums2,nums1];
    }//此时我们已经把短数组放在了前面

    //2.我们定义两个数组的长度，数组长度是常量，不变的所以我们就要用const来定义
    const m = nums1.length;
    const n = nums2.length;
    //3.要确定两个数组的分割线的位置，我们采取假合并的方式
    let left: number = 0;
    let right: number = m;
    while(left <= right){
        //4.此时我们要开始找nums1的分割线
        const i = Math.floor((left + right) / 2);
        //5.确定nums2的分割线位置
        const j = Math.floor((m + n + 1) / 2) - i;

        //当我们找到分割线的位置，那么我们就要获取数据
        //6.从nums1开始获取
        let maxLeft1: number;
        if(i == 0){
            //说明nums1全部被分到了右边，左侧没有数据
            //取得切口左右最近元素，越界用“无穷”补
            maxLeft1 = Number.MIN_SAFE_INTEGER;
        }else{
            maxLeft1 = nums1[i - 1];
        }

        let minRight1: number;
        if(i == m){
            //说明nums1全部被分到了左边，右侧没有数据
            minRight1 = Number.MAX_SAFE_INTEGER;
        }else{
            minRight1 = nums1[i];
        }

        let maxLeft2: number;
        if(j == 0){
            maxLeft2 = Number.MIN_SAFE_INTEGER;
        }else{
            maxLeft2 = nums2[j - 1];
        }

        let minRight2: number;
        if(j == n){
            minRight2 = Number.MAX_SAFE_INTEGER;
        }else{
            minRight2 = nums2[j];
        }

        //7.当我们找到之后，我们要对这个进行检验是否正确
        //左侧最大值 <= 右侧最小值
        if(maxLeft1 <= minRight2 && maxLeft2 <= minRight1){
            //当我们此时找到了分割线，我们要判断数组合在一起的长度是不是为偶数
            if((m + n) % 2 == 0){
                //此时为偶数，我们要返回左侧最大值的平均值+右侧最小值的平均数 的平均数
                return (Math.max(maxLeft1, maxLeft2) + Math.min(minRight1, minRight2)) / 2;
            }else{
                //此时为奇数，我们要返回的是左侧最大值
                return Math.max(maxLeft1, maxLeft2);
            }
        }else if(maxLeft1 > minRight2){
            //此时说明分割线位置不对，太靠右了
            right = i - 1;
        }else{
            //此时是分割线位置不对，太靠左了
            left = i + 1;
        }
    }

    //这里就是程序不会执行到这里，返回0，或者抛出一个异常
    return 0;
    
};
```

## 解题心得：

这道题最难的点就在于我们如何找到一个合适的位置来做分割线

i = (left + right ) / 2---->确定了i的分割线位置

j = (m + n + 1) / 2 - i---->m+n表示的是数组合并后的长度

    +1是为了让左侧长度比右侧多一个

    - i是：我们已经从nums1里面截取了i个出来了，剩下由nums2补齐

    从左侧的数据来看，已经有i个元素去了左边区域，那么就还得有j个元素去左边

    在后面进行判断的时候，i变大，那么j就变小，i和j共同构成了左侧区域
