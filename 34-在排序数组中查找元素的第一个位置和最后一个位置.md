# 34-在排序数组中查找元素的第一个位置和最后一个位置

## 题目描述：

给你一个按照非递减顺序排列的整数数组 `nums`，和一个目标值 `target`。请你找出给定目标值在数组中的开始位置和结束位置。

如果数组中不存在目标值 `target`，返回 `[-1, -1]`。

你必须设计并实现时间复杂度为 `O(log n)` 的算法解决此问题。

**示例 1：**

    输入：**nums = [`5,7,7,8,8,10]`, target = 8
    输出：**[3,4]

**示例 2：**

    输入：**nums = [`5,7,7,8,8,10]`, target = 6
    输出：**[-1,-1]

**示例 3：**

    输入：**nums = [], target = 0
    输出：**[-1,-1]

**提示：**

* `0 <= nums.length <= 105`
* `-109 <= nums[i] <= 109`
* `nums` 是一个非递减数组
* `-109 <= target <= 109` 

## 解题思路：

我们拿到题目先看，这是一个排序数组，题目给定了时间复杂度为`O(log n)`，所以我们就不可以用基本查询来实现这道题，我们应该想到用二分法来思考，我们首先要考虑的是数组为空的这个异常情况，并且题目的输出要求中也明示了为空的情况下，要返回一个数组[-1,-1]。我们先通过二分查找来找到target，然后在向左边界和有边界进行遍历，找到我们的另一个target，返回时的注意点就是，不是返回[start,end]，而是返回[left,right]，这样就分别代表了target的起始位置和结束位置。

接下来我用Java和TypeScript分别来实现这道题

## 1)Java

```java
class Solution {
    public int[] searchRange(int[] nums, int target) {
        /*
            从题目来看，数组是升序排序，且告诉我们时间复杂度，所以我们在查找target时，应使用二分查找的方式进行查找，
            因为要找到target的起始位置和结束位置，所以我们在第一次的二分查找找到target的时候，就要分两种情况来判断
            1：target左侧有2：target右侧有
            根据题目要求，应该先考虑数组为空的情况
            注意方法的返回值是一个数组
        */
        //处理数组为空这个异常情况
        if(nums == null || nums.length == 0){
            return new int[]{-1,-1};
        }
        //通过二分查找先找到这个target,while循环来控制，我们要定义起始和结束索引，要注意计算mid的值
        int start = 0;
        int end = nums.length - 1;
        while(start <= end){
            int mid = start + (end - start) / 2;
            if(target == nums[mid]){
                //此时是我们找到target，要对target左右两个边界的数字进行判断，我们要向左和向右分别进行查找
                //其实这里的left和right就是我们在查找左右两边界时定义的起始位置，我们是反向查找
                int left = mid;
                int right = mid;
                //继续通过while向左边界进行查找，这里left>0就是要防止数组左侧越界
                while(left > 0 && nums[left - 1] == target){
                    left--;
                }
                //继续通过while向右边界进行查找，right<nums.length-1也是防止数组右边界越界
                while(right < nums.length - 1 && nums[right + 1] == target){
                    right++;
                }
                //当我们找到两个target的起始位置和结束位置时，按要求返回数组
                return new int[]{left,right};

            }else if(target > nums[mid]){
                //当target > arr[mid]时，说明target在右侧，end不变，start = mid + 1
                start = mid + 1;
            }else{
                //当target < arr[mid]时，说明target在左侧，start不变，end = mid - 1
                end = mid - 1;
            }
        }
        //当我们整个程序结束以后，都没有找到target，按照题目要求，返回一个[-1,-1]
        return new int[]{-1,-1};
    }
}
```

## 2)TypeScript

```ts
function searchRange(nums: number[], target: number): number[] {
    /*
            从题目来看，数组是升序排序，且告诉我们时间复杂度，所以我们在查找target时，应使用二分查找的方式进行查找，
            因为要找到target的起始位置和结束位置，所以我们在第一次的二分查找找到target的时候，就要分两种情况来判断
            1：target左侧有2：target右侧有
            根据题目要求，应该先考虑数组为空的情况
            注意方法的返回值是一个数组
    */
    //根据题目要求，我们先要考虑数组为空这个异常情况，按要求返回一个新数组[-1,-1]
    if(nums == null || nums.length == 0){
        return [-1,-1];
    }

    //接下来我们通过二分查找来找到数组中的target，条件是start<=end，确保不会错过数组中的每一个元素
    //1.定义start，end索引
    let start: number = 0;
    let end: number = nums.length - 1;//注意结束索引的取值范围
    while(start <= end){
        //2.定义中间索引值mid
        let mid:number = start + Math.floor((end - start) / 2);
        if(target == nums[mid]){
            //此时是找到了数组中target，那么我们就要向左边界和右边界分别进行倒着查找，找到另一个target
            //在这里我们需要注意两个边界数组越界的问题，所以left>0,right<nums.length-1
            //3.我们继续通过while循环来进行查找，这里要注意循环的条件
            //定义左边界和右边界，这里的起始值是mid，从中间向两边开始查找
            let left:number = mid;
            let right:number = mid;
            while(left > 0 && nums[left - 1] == target){
                //依次向左进行遍历查找
                left--;
            }
            while(right < nums.length - 1 && nums[right + 1] == target){
                //依次向右遍历
                right++;
            }
            //此时我们找到了target的起始位置，和结束位置
            return [left,right];
        }else if(target > nums[mid]){
            //当target > nums[mid]时，target在右边，所以end不变，start = mid + 1
            start = mid + 1;
        }else{
            //当target < nums[mid]时，target在左边，所以start不变，end = mid - 1
            end = mid - 1;
        }
    }

    //当我们整个程序都没有找到这个target时，返回[-1,-1]
    return [-1,-1];
};
```

## 解题心得：

我在思考完之后，觉得这道题目的难点其实有以下几个方面：

1.我们要有这种在遍历时分左右边界的这种遍历思想，就是这种查找的题目，我们的思维要灵活一点，就像我在第34题中说过的，二分查找的方法虽然比较容易，但是灵活性很高，要有这种在遍历时区分边界的思想。

2.这个难点也是我在写的时候踩过的一个坑，我们在向左右边界进行查找的时候，要注意这个数组索引越界的问题，我一开始写的时候就没有注意到这个小细节，这里当我们在进行左右边界查找的时候其实这个循环条件不是很好思考，我们的解题逻辑要清晰一点就没有什么大问题。

**给大家分享一个小tips**！

    当我们拿到题目的时候不要一开始就着手写代码，先在纸上整理好思绪，理清整个程序的逻辑，不管是哪种语言，逻辑清晰之后，代码就是体力活。

    在我们分析题目的时候也可以，读一部分题，在纸上写一部分代码，这样更容易思考整个题目，你的思绪不容易乱。


