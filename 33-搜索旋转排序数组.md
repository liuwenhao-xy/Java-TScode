# 33-搜索旋转排序数组

## 题目描述：

整数数组 `nums` 按升序排列，数组中的值 **互不相同** 。

在传递给函数之前，`nums` 在预先未知的某个下标 `k`（`0 <= k < nums.length`）上进行了 **向左旋转**，使数组变为 `[nums[k], nums[k+1], ..., nums[n-1], nums[0], nums[1], ..., nums[k-1]]`（下标 **从 0 开始** 计数）。例如， `[0,1,2,4,5,6,7]` 下标 `3` 上向左旋转后可能变为 `[4,5,6,7,0,1,2]` 。

给你 **旋转后** 的数组 `nums` 和一个整数 `target` ，如果 `nums` 中存在这个目标值 `target` ，则返回它的下标，否则返回 `-1` 。

你必须设计一个时间复杂度为 `O(log n)` 的算法解决此问题。

**示例 1：**

输入：**nums = [4,5,6,7,0,1,2], target = 0**
输出：4

**示例 2：**

输入：**nums = [4,5,6,7,0,1,2], target = 3
输出：**-1

**示例 3：**

输入：**nums = [1], target = 0
输出：**-1

**提示：**

* `1 <= nums.length <= 5000`
* `-104 <= nums[i] <= 104`
* `nums` 中的每个值都 **独一无二**
* 题目数据保证 `nums` 在预先未知的某个下标上进行了旋转
* `-104 <= target <= 104`

## 题目解析：

    这道题我们可能刚读的时候会发懵，不知道题目里面说的旋转数组是什么，而且乍一看这个数组也没有什么规律，我们按照之前在34题中的方法，逐句拆解这个题目。

    首先，我们先看输入和输出这个内容，本身数组是[0,1,2,4,5,6,7]，然后从2处进行了旋转，把前三个数组放到了7后面，此时数组变成了[4,5,6,7,0,1,2]，我们不难看出，7是mid，分左侧和右侧两个区域，左侧和右侧是排序的，那我们就可以先用二分法找到mid，然后再在左侧和右侧进行二分查找后续的遍历就好了。

    可能你到这里已经还想问，这就是一个数组，管他排序不排序，直接找索引遍历就是了，还方便。我们注意看题目的的要求，你必须设计一个时间复杂度为 `O(log n)` 的算法解决此问题。那我们都知道直接遍历的时间复杂度是O(n)，明显不符合要求，所以我们接着读题目的示例，没有找到target的时候返回-1，同时我们在查找之前，也要考虑到数组长度为0，或者数组为空的情况，也是返回-1。**当我们没有思路的时候，多读题，多拆解题目的要求**。

接下来我通过Java和TypeScript来实现这道题目！

## 1)Java

```java
class Solution {
    public int search(int[] nums, int target) {
        /**
            通过二分法先从数组的中间进行查找，把数组分成左右两个部分，接下来我们对数组左右两边进行判断，看哪一边是
            排序数组，如果左边是排序的，那么就要通过target和nums[mid]进行判断，看在数组的左侧还是右侧
            那么右半边是排序的也是同样的操作方式
            我们先要考虑数组为空的这个异常情况，如果异常，按照题目要求返回-1
         */

         //先考虑异常数组为空的情况
         if(nums == null || nums.length == 0){
            return -1;
         }

         //接下来我们通过while循环对整个nums数组进行二分查找，注意查找条件，不放过任何一个元素
         //定义起始索引和结束索引
         int start = 0;
         int end = nums.length - 1;
         while(start <= end){
            //定义中间值索引
            int mid = start + (end - start) / 2;
            if(target == nums[mid]){
                //说明找到了target，直接返回索引
                return mid;
            }

            //接下来我们先假设左半边是有序的
            if(nums[start] <= nums[mid]){
                //这样说明在二分查找结束后左半边是有序的
                //接下来我们要考虑target是在哪一个区域
                if(target < nums[mid] && target >= nums[start]){
                    //此时说明target在左侧区域
                    end = mid - 1;
                }else{
                    //target在右侧区域
                    start = mid + 1;
                }
            }else{
                //这样说明在二分查找结束后右半边是有序的
                if(target > nums[mid] && target <= nums[end]){
                    //说明target在右侧区域
                    start = mid + 1;
                }else{
                    //说明target在左侧区域
                    end = mid - 1;
                }
            }
        }
        //当循环结束没有找到target说明不存在
        return -1;
    }
}
```

## 2)TypeScript

```ts
function search(nums: number[], target: number): number {
        /**
            通过二分法先从数组的中间进行查找，把数组分成左右两个部分，接下来我们对数组左右两边进行判断，看哪一边是
            排序数组，如果左边是排序的，那么就要通过target和nums[mid]进行判断，看在数组的左侧还是右侧
            那么右半边是排序的也是同样的操作方式
            我们先要考虑数组为空的这个异常情况，如果异常，按照题目要求返回-1
        */
        //1.我们先考虑数组为空的情况，返回-1
        if(nums == null || nums.length == 0){
            return -1;
        }

        //2.接下来我们通过二分查找先将数组分成两个区域
        //定义起始索引和结束索引
        let start: number = 0;
        let end:number = nums.length - 1;
        while(start <= end){
            //3.定义中间索引值
            //end - start可以避免我们的mid越求越大
            let mid:number = start + Math.floor((end - start) / 2);
            //4.当我们的target==nums[mid]时，说明我们找到了target
            if(target == nums[mid]){
                return mid;
            }
            //5.我们假设左半边是有序的
            if(nums[start] <= nums[mid]){
                //6.在当前情况下我们在左半边的数组中查找target在哪个区域
                if(target < nums[mid] && target >= nums[start]){
                    //此时说明target在左侧区域，start不变，end = mid - 1
                    end = mid - 1;
                }else{
                    //此时说明target在右侧区域，end不变，start = start + 1
                    start = mid + 1;
                }
            }else{
                //7.在当前情况下，我们假设右半边是有序的
                if(target > nums[mid] && target <= nums[end]){
                    //此时说明target在右侧区域，end不变，start = mid + 1
                    start = mid + 1;
                }else{
                    //此时说明target在左侧区域，start不变，end = mid - 1
                    end = mid - 1;
                }
            }
        }
      //当我们整个循环结束都没有找到target，我们就返回-1
      return -1;  
};
```

## 解题心得：

    当我们把数组分成左右两个区域的时候，要注意判断左右是否为排序数组时的判断条件，不要造成数组越界和少遍历数组元素的情况，因为我开始写的时候，就出现了左侧数组越界和左右区域数组元素漏遍历的情况，导致只能通过一部分测试案例。在左侧区域和右侧区域判断完是否是排序数组的时候，我们的逻辑就更清晰了，和最开始的查找没有什么区别，注意每次的mid索引就好。

    这道题的难点其实率先在于我们对左侧区域的条件判断nums[start] < nums[mid]，因为我们要判定左半边区域数组有序，所以呢就要思考这个条件，else那就是有右侧区域有序。

    有序之后我们直接查找就可以了，每次判断的条件一定要思考几个问题：

* 数组越界问题

* 遍历时的少元素问题


