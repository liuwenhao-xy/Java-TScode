# 153-寻找旋转数组中的最小值

## 题目描述

已知一个长度为 `n` 的数组，预先按照升序排列，经由 `1` 到 `n` 次 **旋转** 后，得到输入数组。例如，原数组 `nums = [0,1,2,4,5,6,7]` 在变化后可能得到：

* 若旋转 `4` 次，则可以得到 `[4,5,6,7,0,1,2]`
* 若旋转 `7` 次，则可以得到 `[0,1,2,4,5,6,7]`

注意，数组 `[a[0], a[1], a[2], ..., a[n-1]]` **旋转一次** 的结果为数组 `[a[n-1], a[0], a[1], a[2], ..., a[n-2]]` 。

给你一个元素值 **互不相同** 的数组 `nums` ，它原来是一个升序排列的数组，并按上述情形进行了多次旋转。请你找出并返回数组中的 **最小元素** 。

你必须设计一个时间复杂度为 `O(log n)` 的算法解决此问题。

**示例 1：**

输入：**nums = [3,4,5,1,2]**
输出：1
解释：原数组为 [1,2,3,4,5] ，旋转 3 次得到输入数组。

**示例 2：**

输入：nums = [4,5,6,7,0,1,2]
输出：0
解释：原数组为 [0,1,2,4,5,6,7] ，旋转 4 次得到输入数组。

**示例 3：**

输入：nums = [11,13,15,17]
输出：11
解释：原数组为 [11,13,15,17] ，旋转 4 次得到输入数组。

**提示：**

* `n == nums.length`
* `1 <= n <= 5000`
* `-5000 <= nums[i] <= 5000`
* `nums` 中的所有整数 **互不相同**
* `nums` 原来是一个升序排序的数组，并进行了 `1` 至 `n` 次旋转

## 题目解析：

    这道题和我们之前的搜索旋转排序数组这道题很类似，就是要在这个数组里面找到这个最小值。主要思想其实没有太大的区别，我们通过题目中所描述的，给定的输入数组nums一定是经过旋转原来数组得到的，那么我们算法思想就很明确了，就是要通过二分查来实现。

    我们先对题目进行分析来看，他让我们找最小值，我们看到示例中的描述，最小值的位置前面一定是一个断点，由于不管哪个位置都是有序排列的，所以只要是在那个断点位置，他**断点右侧的元素就一定是这个最小值min**。这个其实看着很简单易懂，但是就是我们查找的一个关键切入点。

    就是这道题我们在通过循环来实现之前，我们要考虑清除那些异常的情况。

接下来我通过Java和TypeScript两种语言来实现这个题目！

## 1)Java

```java
class Solution {
    public int findMin(int[] nums) {
        /** 
            我们通过题目可以看到，输入数组都是经过原数组经过多次旋转得到的，这就可以得到一个问题，哪怕nums是按升序排好的
            那么他肯定是经过旋转得到的，通过时间复杂度和题目要求我们可以知道，要用二分法来对这个数组进行遍历查找。
            我们看题目中的示例可以发现一个点，就是最小值出现的那个位置的前一个数一定比他大。那么这个点就可以作为二分法的
            切入点。
        */
        //1.先处理数组的异常情况，注意题目的返回值就好了
        if(nums == null || nums.length == 0){
            return -1;
        }
        //漏掉了一个情况，当我们数组中只有一个元素时，直接返回数组该元素
        if(nums.length == 1){
            return nums[0];
        }
        //如果数组是排序好的，也是直接返回当前第一个元素
        if(nums[0] < nums[nums.length - 1]){
            return nums[0];
        }

        //2.接下来我们通过while循环来进行遍历
        //定义起始索引，结束索引
        int start = 0;
        int end = nums.length - 1;
        //确保不会漏掉元素
        while(start <= end){
            //定义中间索引值
            int mid = start + (end - start) / 2;//这样可以避免mid值越来越大

            //3.当nums[mid] > nums[mid+1]时，我么就找到了那个断点位置，因为数组是升序的 大于mid+1，就一定大于后面
            //所有的数
            if(nums[mid] > nums[mid + 1]){
                return nums[mid + 1];
            }
            //4.当我们mid恰好是最小值的时候，此时要注意这个判断条件
            if(mid > 0 && nums[mid] < nums[mid - 1] && nums[mid] < nums[mid + 1]){
                //此时mid就是最小值，我们也考虑到了数组越界的问题
                return nums[mid];
            }
            //5.接下来我们就要判断最小值是在mid的左侧还是右侧区域的问题
            if(nums[mid] > nums[0]){
                //当nums[mid] > nums[0]时，说明此时mid大于他左侧的所有值，min只能在他的右侧区域
                //右侧区域恰好又是排序的
                start = mid + 1;
            }else{
                //当nums[mid] < nums[0]时，说明此时mid小于他左侧的所有值
                end = mid - 1;
            }

        }
        //这道题倒是不会像我们之前碰到的会找不到数组中没有元素的情况
        return nums[0];
    }
}
```

## 2)TypeScript

```ts
function findMin(nums: number[]): number {
        /** 
            我们通过题目可以看到，输入数组都是经过原数组经过多次旋转得到的，这就可以得到一个问题，哪怕nums是按升序排好的
            那么他肯定是经过旋转得到的，通过时间复杂度和题目要求我们可以知道，要用二分法来对这个数组进行遍历查找。
            我们看题目中的示例可以发现一个点，就是最小值出现的那个位置的前一个数一定比他大。那么这个点就可以作为二分法的
            切入点。
        */
        /*这道题目在开始的时候我们要把异常情况考虑的全面一些，
            1.数组为空或者长度为0时，我们返回的是-1，
            2.当数组只有一个元素时，直接返回这个元素就可以了nums[0]，
            3.当数组直接是按序排好的，那么我们也是直接就返回nums[0]。        
        */
        //1. 1.数组为空或者长度为0时，我们返回的是-1
        if(nums == null || nums.length == 0){
            return -1;
        }
        //2.当数组只有一个元素时，直接返回这个元素就可以了nums[0]
        if(nums.length == 1){
            return nums[0];
        }
        //3.当数组直接是按序排好的，那么我们也是直接就返回nums[0]
        if(nums[0] < nums[nums.length - 1]){
            return nums[0];
        }
        //4.我们通过while循环对整个数组进行二分法的遍历
        //定义起始索引和结束索引
        let start:number = 0;
        let end:number = nums.length - 1;
        //我们要确保不会漏掉数组中的任何一个元素
        while(start <= end){
            //5.定义中间值
            let mid:number = start + Math.floor((end - start) / 2);
            //6.当我们找到那个断点的时候，就说明了断点右侧的元素一定是最小值，因为不管怎么旋转，数组都是有序数组
            //我们在判断的时候注意条件就好
            if(nums[mid] > nums[mid + 1]){
                //这个时候，mid+1他就是整个数组中的最小值
                return nums[mid + 1];
            }
            //7.假设我们mid就是最小值，此时我们要注意这个判断条件
            if(mid > 0 && nums[mid] < nums[mid - 1] && nums[mid] < nums[mid + 1]){
                //这个时候我们直接返回mid
                return nums[mid];
            }
            //8.在我们考虑了那些特殊情况的时候，接下来我们就要考虑，在二分查找之后，最小值可能是在左边区域还是右边区域
            if(nums[mid] > nums[0]){
                //此时，mid中间值大于左侧区域的所有数，那么最小值肯定就在右侧区域
                start = mid + 1;
            }else{
                //此时，mid中间值大于右侧区域的所有数，那么最小值肯定就在左侧区域
                end = mid - 1;
            }
        }

    //这道题有一个特殊的点就是我们要找的数一定是在这个数组中的所以程序绝对不会执行到这一步
    return nums[0];
};
```

## 解题心得：

    这道题目其实当我们初看的时候可能觉得就是求一个旋转数组的最小值，看着比较简单，但是当你深度思考这个问题的时候，他其实考虑的点有很多，例如我第一次用Java来写的时候，我就遗漏了当他本身是排序数组和数组长度为1的这两个情况。看似简单但是有很多细节值得我们去思考，我个人来说吧，这道题的难点就在于你的思维能不能转到判断区域的问题上来，因为我们不一定找到一个旋转点他就是断点，所以我们要判断min在哪个区域的问题。
